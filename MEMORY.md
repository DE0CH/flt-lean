- [Price the consumer's input, not the named theory](flt-price-the-consumers-input.md) — the proven downstream bridge often takes a strictly WEAKER input (a K-algebra MAP, not an isomorphism); read its signature before building the classical theorem a docstring names
- [The port may exist in an unimported sibling](flt-port-may-exist-in-unimported-sibling.md) — a module can be green and counted (reachable from the root) yet invisible from the file that needs it; a 6500-line port leaf was two-thirds already proven in `ProjectiveModelOverField.lean`, closed by one `public import`
- [A route saying "return the discarded datum" may already be receiving it](flt-route-says-return-discarded-datum.md) — steps 1 and 2 of a three-step route had landed one release earlier; the residue was 15 lines and the tell was a comment at the discard site
- [`apply`, not `refine f _ x y`, when the goal pins a metavariable](lean-apply-not-refine-when-goal-pins-metavariable.md) — `refine` elaborates arguments before unifying with the goal, so an argument whose type mentions an unresolved implicit fails on two identical-looking types
