## A WITNESS THAT REFUTES THE CLAUSE YOU REMEMBER MAY NOT SATISFY THE CLAUSE THAT IS WRITTEN

(Same run, and it is the reason no audit block was added.)  The natural next shortcut on that
file's leaf is *"the fibre clause already forces `Module.Projective (range d)`, so the leaf has
a redundant conjunct"*, and the natural refutation is `R = k[ε]/(ε²)`, `d = ε : R ⟶ R`, whose
`ker(d ⊗ k)` is one-dimensional while `range d = εR` is not projective.  That witness is
correct about *constant fibre dimension* and **does not satisfy the clause the leaf actually
states**, which is `comap d (𝔪·⊤) = span {e 1} ⊔ 𝔪·⊤` — a demand that the fibre kernel be
spanned by the reduction of a GLOBAL element, hence that `ker d ⊄ 𝔪·R^{n₀}`.  Here
`ker d = εR ⊆ 𝔪R`, so the clause fails and the witness refutes nothing.

**The check is the one this file already prescribes one section down, applied to your own
draft**: instantiate the witness and verify EVERY surviving clause, including the one you are
not attacking.  Writing the audit down without it would have retired a genuinely open
question — whether the projectivity conjunct is redundant — on false evidence, which is worse
than leaving it open.  It is queued as a question instead.

